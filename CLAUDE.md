# CLAUDE CODE INSTRUCTIONS & RULES -- AQA EVENTS CARD SYSTEM

## CRITICAL: READ-ONLY SYSTEM & SEPARATE PROJECT

This repository is the **AQA Events Card System**, an independent, live production system with financial data.

---

### RULE 1: SEPARATE PROJECT SYSTEM
- This directory is completely SEPARATE from `aqasportsdotpro` and all other projects.
- NEVER apply changes, fixes, or instructions intended for `aqasportsdotpro` to this directory.
- NEVER assume tasks in other folders include this directory.

---

### RULE 2: STRICT EDIT BAN (READ-ONLY BY DEFAULT)
- **DO NOT EDIT, CREATE, RENAME, OR DELETE ANY FILE WITHOUT EXPLICIT USER APPROVAL.**
- You must operate in READ-ONLY mode.
- If asked to modify code, you must first present the exact plan or diff and wait for explicit approval before writing to any file.

---

### RULE 3: NEVER COMMIT OR PUSH
- **NEVER** run `git commit`, `git push`, or deploy commands unless the user explicitly commands it in the current turn.

---

### RULE 4: DATABASE & PRODUCTION SAFETY
- NEVER modify `prisma/schema.prisma` without explicit permission.
- NEVER run `prisma db push`, `prisma migrate reset`, `prisma migrate dev`, or any seed script.
- NEVER touch the live production database.

---

### RULE 5: CODE & TEXT STANDARDS
- **NO emojis anywhere** (no emojis in code, comments, commit messages, or responses).
- **Western Arabic numerals only (0-9)** across all languages and translations.
